<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "OpenAdTools | Open-Source Tools for Performance Marketers." %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Open-Source Tools for Performance Marketers. No Purchase Necessary, No Login Required and No Personal Data Stored." %>">
  <meta name="google-site-verification" content="wUGytZ0fj4hSIQ6b3f7XrxrYPRLnAk7-YdqcnCE2NZs" />
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  <%= favicon_link_tag asset_path('favicon.ico') %>
  <%= yield(:head) %>
</head>
<body>

	<%= render 'layouts/navbar' %>

  <div class="wrapper">

  	<div class="container">

  		<%= render 'layouts/messages' %>

      <a name="top"></a>

  	  <%= yield %>

      <%= render 'layouts/disclaimer' %>

  	</div><!-- /.container -->
    <div class="push"></div>
  </div>

  <br>

	<%= render 'layouts/footer' %>

  <script>
    (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
    (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
    m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
    })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

    ga('create', 'UA-50065936-1', 'openadtools.com');
    ga('require', 'displayfeatures');
    ga('send', 'pageview');

    var actionLabel = window.location.pathname;

    // Button Event Track - every button has class "submit btn"
    $('.submit.btn').on('click', function() {
      actionLabel = window.location.pathname;
      ga('send', 'event', 'tool', 'button-submit', actionLabel);
    });

    // Track Navbar Signup Button
    $('#newsletter-signup').on('click', function() {
      ga('send', 'event', 'subscribe', 'start', 'navbar');
    });

  </script>

</body>
</html>
